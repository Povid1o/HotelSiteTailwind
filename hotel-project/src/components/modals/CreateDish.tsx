import { observer } from "mobx-react-lite";

const CreateDish = observer(() => {
  return (
    <div>
      <h2>Create Dish Modal</h2>
      {/* Add your form and logic here */}
    </div>
  );
}
);

export default CreateDish;